# AGENTS.md

## Merge and rebase conflicts

When resolving merge/rebase conflicts, preserve the intent of BOTH sides.
Never take one side wholesale without explaining what the other side did and why it's safe to drop.
Run `scripts/verify.sh full` after resolving.
