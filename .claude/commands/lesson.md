Something went wrong in this session. Append an entry to docs/agent-log.md:
- Date, task (spec/plan ID if any)
- What happened (1-2 sentences, facts only)
- Root-cause layer: context | enforcement | spec | coordination | capability
- Why that layer didn't prevent it
- Proposed fix: the smallest MECHANICAL fix (lint rule, type, test, hook, verify.sh check).
  Propose a prose rule only if no mechanical option exists.
Do not apply the fix. I will decide.
